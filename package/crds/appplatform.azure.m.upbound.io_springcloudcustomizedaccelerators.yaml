---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: springcloudcustomizedaccelerators.appplatform.azure.m.upbound.io
spec:
  group: appplatform.azure.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - azure
    kind: SpringCloudCustomizedAccelerator
    listKind: SpringCloudCustomizedAcceleratorList
    plural: springcloudcustomizedaccelerators
    singular: springcloudcustomizedaccelerator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SpringCloudCustomizedAccelerator is the Schema for the SpringCloudCustomizedAccelerators
          API. Manages a Spring Cloud Customized Accelerator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SpringCloudCustomizedAcceleratorSpec defines the desired
              state of SpringCloudCustomizedAccelerator
            properties:
              forProvider:
                properties:
                  acceleratorTags:
                    description: Specifies a list of accelerator tags.
                    items:
                      type: string
                    type: array
                  acceleratorType:
                    description: Specifies the type of the Spring Cloud Customized
                      Accelerator. Possible values are Accelerator and Fragment. Defaults
                      to Accelerator.
                    type: string
                  description:
                    description: Specifies the description of the Spring Cloud Customized
                      Accelerator.
                    type: string
                  displayName:
                    description: Specifies the display name of the Spring Cloud Customized
                      Accelerator..
                    type: string
                  gitRepository:
                    description: A git_repository block as defined below.
                    properties:
                      basicAuth:
                        description: A basic_auth block as defined below. Conflicts
                          with git_repository[0].ssh_auth. Changing this forces a
                          new Spring Cloud Customized Accelerator to be created.
                        properties:
                          passwordSecretRef:
                            description: Specifies the password of git repository
                              basic auth.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          username:
                            description: Specifies the username of git repository
                              basic auth.
                            type: string
                        type: object
                      branch:
                        description: Specifies the Git repository branch to be used.
                        type: string
                      caCertificateId:
                        description: Specifies the ID of the CA Spring Cloud Certificate
                          for https URL of Git repository.
                        type: string
                      commit:
                        description: Specifies the Git repository commit to be used.
                        type: string
                      gitTag:
                        description: Specifies the Git repository tag to be used.
                        type: string
                      intervalInSeconds:
                        description: Specifies the interval for checking for updates
                          to Git or image repository. It should be greater than 10.
                        type: number
                      path:
                        description: Specifies the path under the git repository to
                          be treated as the root directory of the accelerator or the
                          fragment (depending on accelerator_type).
                        type: string
                      sshAuth:
                        description: A ssh_auth block as defined below. Conflicts
                          with git_repository[0].basic_auth. Changing this forces
                          a new Spring Cloud Customized Accelerator to be created.
                        properties:
                          hostKeyAlgorithm:
                            description: Specifies the SSH Key algorithm of git repository
                              basic auth.
                            type: string
                          hostKeySecretRef:
                            description: Specifies the Public SSH Key of git repository
                              basic auth.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          privateKeySecretRef:
                            description: Specifies the Private SSH Key of git repository
                              basic auth.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        type: object
                      url:
                        description: Specifies Git repository URL for the accelerator.
                        type: string
                    type: object
                  iconUrl:
                    description: Specifies the icon URL of the Spring Cloud Customized
                      Accelerator..
                    type: string
                  springCloudAcceleratorId:
                    description: The ID of the Spring Cloud Accelerator. Changing
                      this forces a new Spring Cloud Customized Accelerator to be
                      created.
                    type: string
                  springCloudAcceleratorIdRef:
                    description: Reference to a SpringCloudAccelerator in appplatform
                      to populate springCloudAcceleratorId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  springCloudAcceleratorIdSelector:
                    description: Selector for a SpringCloudAccelerator in appplatform
                      to populate springCloudAcceleratorId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  acceleratorTags:
                    description: Specifies a list of accelerator tags.
                    items:
                      type: string
                    type: array
                  acceleratorType:
                    description: Specifies the type of the Spring Cloud Customized
                      Accelerator. Possible values are Accelerator and Fragment. Defaults
                      to Accelerator.
                    type: string
                  description:
                    description: Specifies the description of the Spring Cloud Customized
                      Accelerator.
                    type: string
                  displayName:
                    description: Specifies the display name of the Spring Cloud Customized
                      Accelerator..
                    type: string
                  gitRepository:
                    description: A git_repository block as defined below.
                    properties:
                      basicAuth:
                        description: A basic_auth block as defined below. Conflicts
                          with git_repository[0].ssh_auth. Changing this forces a
                          new Spring Cloud Customized Accelerator to be created.
                        properties:
                          passwordSecretRef:
                            description: Specifies the password of git repository
                              basic auth.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          username:
                            description: Specifies the username of git repository
                              basic auth.
                            type: string
                        required:
                        - passwordSecretRef
                        type: object
                      branch:
                        description: Specifies the Git repository branch to be used.
                        type: string
                      caCertificateId:
                        description: Specifies the ID of the CA Spring Cloud Certificate
                          for https URL of Git repository.
                        type: string
                      commit:
                        description: Specifies the Git repository commit to be used.
                        type: string
                      gitTag:
                        description: Specifies the Git repository tag to be used.
                        type: string
                      intervalInSeconds:
                        description: Specifies the interval for checking for updates
                          to Git or image repository. It should be greater than 10.
                        type: number
                      path:
                        description: Specifies the path under the git repository to
                          be treated as the root directory of the accelerator or the
                          fragment (depending on accelerator_type).
                        type: string
                      sshAuth:
                        description: A ssh_auth block as defined below. Conflicts
                          with git_repository[0].basic_auth. Changing this forces
                          a new Spring Cloud Customized Accelerator to be created.
                        properties:
                          hostKeyAlgorithm:
                            description: Specifies the SSH Key algorithm of git repository
                              basic auth.
                            type: string
                          hostKeySecretRef:
                            description: Specifies the Public SSH Key of git repository
                              basic auth.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          privateKeySecretRef:
                            description: Specifies the Private SSH Key of git repository
                              basic auth.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        required:
                        - privateKeySecretRef
                        type: object
                      url:
                        description: Specifies Git repository URL for the accelerator.
                        type: string
                    type: object
                  iconUrl:
                    description: Specifies the icon URL of the Spring Cloud Customized
                      Accelerator..
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.gitRepository is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.gitRepository)
                || (has(self.initProvider) && has(self.initProvider.gitRepository))'
          status:
            description: SpringCloudCustomizedAcceleratorStatus defines the observed
              state of SpringCloudCustomizedAccelerator.
            properties:
              atProvider:
                properties:
                  acceleratorTags:
                    description: Specifies a list of accelerator tags.
                    items:
                      type: string
                    type: array
                  acceleratorType:
                    description: Specifies the type of the Spring Cloud Customized
                      Accelerator. Possible values are Accelerator and Fragment. Defaults
                      to Accelerator.
                    type: string
                  description:
                    description: Specifies the description of the Spring Cloud Customized
                      Accelerator.
                    type: string
                  displayName:
                    description: Specifies the display name of the Spring Cloud Customized
                      Accelerator..
                    type: string
                  gitRepository:
                    description: A git_repository block as defined below.
                    properties:
                      basicAuth:
                        description: A basic_auth block as defined below. Conflicts
                          with git_repository[0].ssh_auth. Changing this forces a
                          new Spring Cloud Customized Accelerator to be created.
                        properties:
                          username:
                            description: Specifies the username of git repository
                              basic auth.
                            type: string
                        type: object
                      branch:
                        description: Specifies the Git repository branch to be used.
                        type: string
                      caCertificateId:
                        description: Specifies the ID of the CA Spring Cloud Certificate
                          for https URL of Git repository.
                        type: string
                      commit:
                        description: Specifies the Git repository commit to be used.
                        type: string
                      gitTag:
                        description: Specifies the Git repository tag to be used.
                        type: string
                      intervalInSeconds:
                        description: Specifies the interval for checking for updates
                          to Git or image repository. It should be greater than 10.
                        type: number
                      path:
                        description: Specifies the path under the git repository to
                          be treated as the root directory of the accelerator or the
                          fragment (depending on accelerator_type).
                        type: string
                      sshAuth:
                        description: A ssh_auth block as defined below. Conflicts
                          with git_repository[0].basic_auth. Changing this forces
                          a new Spring Cloud Customized Accelerator to be created.
                        properties:
                          hostKeyAlgorithm:
                            description: Specifies the SSH Key algorithm of git repository
                              basic auth.
                            type: string
                        type: object
                      url:
                        description: Specifies Git repository URL for the accelerator.
                        type: string
                    type: object
                  iconUrl:
                    description: Specifies the icon URL of the Spring Cloud Customized
                      Accelerator..
                    type: string
                  id:
                    description: The ID of the Spring Cloud Customized Accelerator.
                    type: string
                  springCloudAcceleratorId:
                    description: The ID of the Spring Cloud Accelerator. Changing
                      this forces a new Spring Cloud Customized Accelerator to be
                      created.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
