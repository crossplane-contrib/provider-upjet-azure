apiVersion: network.azure.upbound.io/v1beta1
kind: VirtualWAN
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
