apiVersion: compute.azure.upbound.io/v1beta1
kind: SSHPublicKey
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    publicKey: ...
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
