apiVersion: compute.azure.upbound.io/v1alpha2
kind: SharedImage
metadata:
  name: example
spec:
  forProvider:
    galleryName: example
    identifier:
    - offer: OfferName
      publisher: PublisherName
      sku: ExampleSku
    location: West Europe
    osType: Linux
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
