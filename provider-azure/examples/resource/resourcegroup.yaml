apiVersion: azure.upbound.io/v1beta1
kind: ResourceGroup
metadata:
  name: test-official-rg
spec:
  forProvider:
    location: "East US"
    tags:
      provisioner: upbound
  providerConfigRef:
    name: default