apiVersion: azure.upbound.io/v1beta1
kind: ResourceGroup
metadata:
  name: test-official-providers
spec:
  forProvider:
    location: "East US"
    tags:
      provisioner: crossplane
  providerConfigRef:
    name: default