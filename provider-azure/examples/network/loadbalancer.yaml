apiVersion: network.azure.upbound.io/v1beta1
kind: LoadBalancer
metadata:
  name: example-lb
spec:
  forProvider:
    location: East US
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example