apiVersion: windows.azure.upbound.io/v1alpha1
kind: FunctionAppSlot
metadata:
  name: example
spec:
  forProvider:
    functionAppId: ${azurerm_windows_function_app.example.id}
    name: example-slot
    siteConfig: {}
    storageAccountName: ${azurerm_storage_account.example.name}
  providerConfigRef:
    name: example
