apiVersion: web.azure.upbound.io/v1alpha1
kind: AppServiceVirtualNetworkSwiftConnection
metadata:
  name: example
spec:
  forProvider:
    appServiceId: ${azurerm_app_service.example.id}
    subnetIdRef:
      name: example
  providerConfigRef:
    name: example
