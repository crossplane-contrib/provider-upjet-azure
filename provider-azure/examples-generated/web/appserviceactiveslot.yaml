apiVersion: web.azure.upbound.io/v1beta1
kind: AppServiceActiveSlot
metadata:
  name: example
spec:
  forProvider:
    appServiceName: example-app-service
    appServiceSlotName: ${random_id.server.hex}
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
