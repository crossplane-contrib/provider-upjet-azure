apiVersion: web.azure.upbound.io/v1alpha1
kind: SourceControlToken
metadata:
  name: example
spec:
  forProvider:
    tokenSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
    type: GitHub
  providerConfigRef:
    name: example
