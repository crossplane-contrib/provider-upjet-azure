apiVersion: web.azure.upbound.io/v1beta1
kind: AppActiveSlot
metadata:
  name: example
spec:
  forProvider:
    slotId: ${azurerm_windows_web_app_slot.example.id}
  providerConfigRef:
    name: example
