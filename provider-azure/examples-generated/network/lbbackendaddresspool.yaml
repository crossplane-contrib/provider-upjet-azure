apiVersion: network.azure.upbound.io/v1alpha1
kind: LBBackendAddressPool
metadata:
  name: example
spec:
  forProvider:
    loadbalancerId: ${azurerm_lb.example.id}
    name: BackEndAddressPool
  providerConfigRef:
    name: example
