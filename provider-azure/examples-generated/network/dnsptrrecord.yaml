apiVersion: network.azure.upbound.io/v1beta1
kind: DNSPTRRecord
metadata:
  name: example
spec:
  forProvider:
    name: test
    records:
    - yourdomain.com
    resourceGroupNameRef:
      name: example
    ttl: 300
    zoneName: mydomain.com
  providerConfigRef:
    name: example
