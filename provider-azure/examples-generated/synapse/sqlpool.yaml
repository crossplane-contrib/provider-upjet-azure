apiVersion: synapse.azure.upbound.io/v1alpha1
kind: SQLPool
metadata:
  name: example
spec:
  forProvider:
    createMode: Default
    name: examplesqlpool
    skuName: DW100c
    synapseWorkspaceId: ${azurerm_synapse_workspace.example.id}
  providerConfigRef:
    name: example
