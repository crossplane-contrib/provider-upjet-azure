apiVersion: synapse.azure.upbound.io/v1beta1
kind: WorkspaceKey
metadata:
  name: example
spec:
  forProvider:
    active: true
    customerManagedKeyName: enckey
    customerManagedKeyVersionlessId: ${azurerm_key_vault_key.example.versionless_id}
    synapseWorkspaceId: ${azurerm_synapse_workspace.example.id}
  providerConfigRef:
    name: example
