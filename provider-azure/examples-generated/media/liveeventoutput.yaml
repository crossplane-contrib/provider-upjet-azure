apiVersion: media.azure.upbound.io/v1beta1
kind: LiveEventOutput
metadata:
  name: example
spec:
  forProvider:
    archive_window_length: PT5M
    assetName: Asset1
    description: Test live output 1
    hlsFragmentsPerTsSegment: 5
    liveEventId: ${azurerm_media_live_event.example.id}
    manifestName: testmanifest
    name: exampleoutput
    outputSnapTimeInSeconds: 0
  providerConfigRef:
    name: example
