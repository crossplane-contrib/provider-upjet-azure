apiVersion: keyvault.azure.upbound.io/v1beta1
kind: Secret
metadata:
  name: example
spec:
  forProvider:
    keyVaultIdRef:
      name: example
    valueSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
