apiVersion: app.azure.upbound.io/v1alpha1
kind: ServiceSlotCustomHostNameBinding
metadata:
  name: example
spec:
  forProvider:
    appServiceSlotId: ${azurerm_app_service_slot.example.id}
    hostname: www.mywebsite.com
  providerConfigRef:
    name: example
