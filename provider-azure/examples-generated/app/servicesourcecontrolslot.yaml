apiVersion: app.azure.upbound.io/v1alpha1
kind: ServiceSourceControlSlot
metadata:
  name: example
spec:
  forProvider:
    branch: master
    repoUrl: https://github.com/Azure-Samples/python-docs-hello-world
    slotId: ${azurerm_linux_web_app_slot.example.id}
  providerConfigRef:
    name: example
