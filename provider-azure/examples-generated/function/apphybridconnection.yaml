apiVersion: function.azure.upbound.io/v1beta1
kind: AppHybridConnection
metadata:
  name: example
spec:
  forProvider:
    functionAppId: ${azurerm_windows_web_app.example.id}
    hostname: myhostname.example
    port: 8081
    relayId: ${azurerm_relay_hybrid_connection.example.id}
  providerConfigRef:
    name: example
