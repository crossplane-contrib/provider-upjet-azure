apiVersion: resource.azure.upbound.io/v1alpha1
kind: PolicyRemediation
metadata:
  name: example
spec:
  forProvider:
    name: remediation1
    policyAssignmentId: ${azurerm_resource_group_policy_assignment.test.id}
    resourceId: ${azurerm_virtual_network.example.id}
  providerConfigRef:
    name: example
