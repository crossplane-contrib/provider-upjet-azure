apiVersion: resource.azure.upbound.io/v1alpha1
kind: PolicyExemption
metadata:
  name: example
spec:
  forProvider:
    exemptionCategory: Mitigated
    name: exemption1
    policyAssignmentId: ${azurerm_resource_policy_assignment.example.id}
    resourceId: ${azurerm_resource_policy_assignment.example.resource_id}
  providerConfigRef:
    name: example
