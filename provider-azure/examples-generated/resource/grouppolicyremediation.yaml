apiVersion: resource.azure.upbound.io/v1alpha1
kind: GroupPolicyRemediation
metadata:
  name: example
spec:
  forProvider:
    locationFilters:
    - West Europe
    policyAssignmentId: ${azurerm_policy_assignment.example.id}
    resourceGroupIdRef:
      name: example
  providerConfigRef:
    name: example
