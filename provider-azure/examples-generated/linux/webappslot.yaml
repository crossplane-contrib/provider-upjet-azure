apiVersion: linux.azure.upbound.io/v1alpha1
kind: WebAppSlot
metadata:
  name: example
spec:
  forProvider:
    appServiceId: ${azurerm_linux_web_app.example.id}
    name: example-slot
    siteConfig:
    - {}
  providerConfigRef:
    name: example
