apiVersion: linux.azure.upbound.io/v1beta1
kind: FunctionApp
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: example-linux-function-app
    resourceGroupNameRef:
      name: example
    servicePlanId: ${azurerm_service_plan.example.id}
    siteConfig:
    - {}
    storageAccountName: ${azurerm_storage_account.example.name}
  providerConfigRef:
    name: example
