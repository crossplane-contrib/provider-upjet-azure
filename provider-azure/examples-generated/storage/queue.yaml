apiVersion: storage.azure.upbound.io/v1alpha1
kind: Queue
metadata:
  name: example
spec:
  forProvider:
    name: mysamplequeue
    storageAccountName: ${azurerm_storage_account.example.name}
  providerConfigRef:
    name: example
