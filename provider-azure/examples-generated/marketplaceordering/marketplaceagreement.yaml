apiVersion: marketplaceordering.azure.upbound.io/v1alpha1
kind: MarketplaceAgreement
metadata:
  name: example
spec:
  forProvider:
    offer: waf
    plan: hourly
    publisher: barracudanetworks
  providerConfigRef:
    name: example
