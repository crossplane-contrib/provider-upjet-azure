apiVersion: servicebus.azure.upbound.io/v1alpha1
kind: SubscriptionRule
metadata:
  name: example
spec:
  forProvider:
    filterType: SqlFilter
    name: tfex_servicebus_rule
    sqlFilter: colour = 'red'
    subscriptionId: ${azurerm_servicebus_subscription.example.id}
  providerConfigRef:
    name: example
