apiVersion: kusto.azure.upbound.io/v1alpha1
kind: Script
metadata:
  name: example
spec:
  forProvider:
    continueOnErrorsEnabled: true
    databaseId: ${azurerm_kusto_database.example.id}
    forceAnUpdateWhenValueChanged: first
    name: example
    sasToken: ${data.azurerm_storage_account_blob_container_sas.example.sas}
    url: ${azurerm_storage_blob.example.id}
  providerConfigRef:
    name: example
