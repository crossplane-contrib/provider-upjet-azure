apiVersion: iothub.azure.upbound.io/v1alpha1
kind: Certificate
metadata:
  name: example
spec:
  forProvider:
    certificateContentSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
    iothubName: ${azurerm_iothub.example.name}
    isVerified: true
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
