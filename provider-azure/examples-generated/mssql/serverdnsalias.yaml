apiVersion: mssql.azure.upbound.io/v1beta1
kind: ServerDNSAlias
metadata:
  name: example
spec:
  forProvider:
    mssqlServerId: ${azurerm_mssql_server.example.id}
    name: example-dns-alias
  providerConfigRef:
    name: example
