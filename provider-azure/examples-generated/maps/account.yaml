apiVersion: maps.azure.upbound.io/v1alpha1
kind: Account
metadata:
  name: example
spec:
  forProvider:
    name: example-maps-account
    resourceGroupNameRef:
      name: example
    skuName: S1
    tags:
      environment: Test
  providerConfigRef:
    name: example
