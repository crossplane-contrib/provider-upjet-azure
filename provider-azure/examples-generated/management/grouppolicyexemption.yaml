apiVersion: management.azure.upbound.io/v1alpha1
kind: GroupPolicyExemption
metadata:
  name: example
spec:
  forProvider:
    exemptionCategory: Mitigated
    managementGroupId: ${azurerm_management_group.example.id}
    name: exemption1
    policyAssignmentId: ${azurerm_management_group_policy_assignment.example.id}
  providerConfigRef:
    name: example
