apiVersion: management.azure.upbound.io/v1alpha2
kind: ManagementGroup
metadata:
  name: example
spec:
  forProvider:
    displayName: ParentGroup
    subscriptionIds:
    - ${data.azurerm_subscription.current.subscription_id}
  providerConfigRef:
    name: example
