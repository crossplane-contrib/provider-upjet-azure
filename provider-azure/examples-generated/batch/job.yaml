apiVersion: batch.azure.upbound.io/v1beta1
kind: Job
metadata:
  name: example
spec:
  forProvider:
    batchPoolId: ${azurerm_batch_pool.example.id}
    name: examplejob
  providerConfigRef:
    name: example
