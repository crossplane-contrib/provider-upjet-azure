apiVersion: api.azure.upbound.io/v1beta1
kind: Connection
metadata:
  name: example
spec:
  forProvider:
    displayName: Example 1
    lifecycle:
    - ignore_changes:
      - parameter_values
    managedApiId: ${data.azurerm_managed_api.example.id}
    name: example-connection
    parameterValues:
      connectionString: ${azurerm_servicebus_namespace.example.default_primary_connection_string}
    resourceGroupNameRef:
      name: example
    tags:
      Hello: World
  providerConfigRef:
    name: example
