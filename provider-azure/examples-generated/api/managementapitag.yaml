apiVersion: api.azure.upbound.io/v1alpha1
kind: ManagementAPITag
metadata:
  name: example
spec:
  forProvider:
    apiId: ${azurerm_api_management_api.example.id}
  providerConfigRef:
    name: example
