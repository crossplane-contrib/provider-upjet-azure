apiVersion: timeseriesinsights.azure.upbound.io/v1alpha1
kind: ReferenceDataSet
metadata:
  name: example
spec:
  forProvider:
    keyProperty:
    - name: keyProperty1
      type: String
    location: West Europe
    name: example
    timeSeriesInsightsEnvironmentId: ${azurerm_iot_time_series_insights_standard_environment.example.id}
  providerConfigRef:
    name: example
