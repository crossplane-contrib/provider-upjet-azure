apiVersion: dbformariadb.azure.upbound.io/v1beta1
kind: VirtualNetworkRule
metadata:
  name: example
spec:
  forProvider:
    resourceGroupNameRef:
      name: example
    serverNameRef:
      name: example
    subnetIdRef:
      name: example
  providerConfigRef:
    name: example
