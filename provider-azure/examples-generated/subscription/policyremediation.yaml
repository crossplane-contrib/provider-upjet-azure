apiVersion: subscription.azure.upbound.io/v1alpha1
kind: PolicyRemediation
metadata:
  name: example
spec:
  forProvider:
    policyAssignmentId: ${azurerm_subscription_policy_assignment.example.id}
    subscriptionId: ${data.azurerm_subscription.example.id}
  providerConfigRef:
    name: example
