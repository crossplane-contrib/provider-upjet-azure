apiVersion: subscription.azure.upbound.io/v1beta1
kind: PolicyExemption
metadata:
  name: example
spec:
  forProvider:
    exemptionCategory: Mitigated
    name: exampleExemption
    policyAssignmentId: ${azurerm_subscription_policy_assignment.example.id}
    resource_group_id: ${data.azurerm_subscription.example.id}
  providerConfigRef:
    name: example
