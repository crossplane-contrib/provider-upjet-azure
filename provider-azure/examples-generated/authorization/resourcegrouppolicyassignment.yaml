apiVersion: authorization.azure.upbound.io/v1alpha2
kind: ResourceGroupPolicyAssignment
metadata:
  name: example
spec:
  forProvider:
    parameters: |2
          {
            "tagName": {
              "value": "Business Unit"
            },
            "tagValue": {
              "value": "BU"
            }
          }
    policyDefinitionId: ${azurerm_policy_definition.example.id}
    resourceGroupIdRef:
      name: example
  providerConfigRef:
    name: example
