apiVersion: sql.azure.upbound.io/v1beta1
kind: MSSQLServerTransparentDataEncryption
metadata:
  name: example
spec:
  forProvider:
    serverIdRef:
      name: example
  providerConfigRef:
    name: example
