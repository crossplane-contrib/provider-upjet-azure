apiVersion: datashare.azure.upbound.io/v1alpha1
kind: DataSetDataLakeGen2
metadata:
  name: example
spec:
  forProvider:
    filePath: myfile.txt
    fileSystemName: ${azurerm_storage_data_lake_gen2_filesystem.example.name}
    shareId: ${azurerm_data_share.example.id}
    storageAccountId: ${azurerm_storage_account.example.id}
  providerConfigRef:
    name: example
