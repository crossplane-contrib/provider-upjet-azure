apiVersion: cosmosdb.azure.upbound.io/v1alpha2
kind: MongoDatabase
metadata:
  name: example
spec:
  forProvider:
    accountNameRef:
      name: example
    resourceGroupNameRef:
      name: example
    throughput: 400
  providerConfigRef:
    name: example
