apiVersion: disk.azure.upbound.io/v1beta1
kind: PoolISCSITarget
metadata:
  name: example
spec:
  forProvider:
    aclMode: Dynamic
    disksPoolId: ${azurerm_disk_pool.example.id}
    name: example
    targetIqn: iqn.2021-11.com.microsoft:test
  providerConfigRef:
    name: example
