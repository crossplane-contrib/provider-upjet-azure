apiVersion: disk.azure.upbound.io/v1beta1
kind: PoolManagedDiskAttachment
metadata:
  name: example
spec:
  forProvider:
    diskPoolId: ${azurerm_disk_pool.example.id}
    managedDiskId: ${azurerm_managed_disk.example.id}
  providerConfigRef:
    name: example
