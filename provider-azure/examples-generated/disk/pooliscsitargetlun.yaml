apiVersion: disk.azure.upbound.io/v1alpha1
kind: PoolISCSITargetLun
metadata:
  name: example
spec:
  forProvider:
    diskPoolManagedDiskAttachmentId: ${azurerm_disk_pool_managed_disk_attachment.example.id}
    iscsiTargetId: ${azurerm_disk_pool_iscsi_target.example.id}
    name: example-disk
  providerConfigRef:
    name: example
