apiVersion: eventhub.azure.upbound.io/v1alpha1
kind: NamespaceDisasterRecoveryConfig
metadata:
  name: example
spec:
  forProvider:
    namespaceName: ${azurerm_eventhub_namespace.primary.name}
    partnerNamespaceId: ${azurerm_eventhub_namespace.secondary.id}
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
