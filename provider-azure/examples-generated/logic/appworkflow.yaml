apiVersion: logic.azure.upbound.io/v1beta1
kind: AppWorkflow
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: workflow1
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
