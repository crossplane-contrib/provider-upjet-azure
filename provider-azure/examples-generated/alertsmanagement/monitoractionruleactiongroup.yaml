apiVersion: alertsmanagement.azure.upbound.io/v1alpha1
kind: MonitorActionRuleActionGroup
metadata:
  name: example
spec:
  forProvider:
    actionGroupId: ${azurerm_monitor_action_group.example.id}
    name: example-amar
    resourceGroupNameRef:
      name: example
    scope:
    - resourceIds:
      - ${azurerm_resource_group.example.id}
      type: ResourceGroup
    tags:
      foo: bar
  providerConfigRef:
    name: example
