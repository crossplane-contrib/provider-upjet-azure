apiVersion: aad.azure.upbound.io/v1beta1
kind: ActiveDirectoryDomainServiceReplicaSet
metadata:
  name: example
spec:
  forProvider:
    domainServiceId: ${azurerm_active_directory_domain_service.example.id}
    location: West Europe
    subnetIdRef:
      name: example
  providerConfigRef:
    name: example
