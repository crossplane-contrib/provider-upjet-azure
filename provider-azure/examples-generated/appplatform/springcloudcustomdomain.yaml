apiVersion: appplatform.azure.upbound.io/v1alpha1
kind: SpringCloudCustomDomain
metadata:
  name: example
spec:
  forProvider:
    name: ${join(".", [azurerm_dns_cname_record.example.name, azurerm_dns_cname_record.example.zone_name])}
    springCloudAppId: ${azurerm_spring_cloud_app.example.id}
  providerConfigRef:
    name: example
