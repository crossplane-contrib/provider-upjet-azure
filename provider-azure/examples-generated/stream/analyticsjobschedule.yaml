apiVersion: stream.azure.upbound.io/v1beta1
kind: AnalyticsJobSchedule
metadata:
  name: example
spec:
  forProvider:
    startMode: CustomTime
    startTime: '%s'
    streamAnalyticsJobId: ${azurerm_stream_analytics_job.test.id}
  providerConfigRef:
    name: example
