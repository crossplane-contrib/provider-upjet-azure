apiVersion: devices.azure.upbound.io/v1alpha1
kind: IOTHubEndpointServiceBusTopic
metadata:
  name: example
spec:
  forProvider:
    connectionString: ${azurerm_servicebus_topic_authorization_rule.example.primary_connection_string}
    iothubId: ${azurerm_iothub.example.id}
    name: example
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example
