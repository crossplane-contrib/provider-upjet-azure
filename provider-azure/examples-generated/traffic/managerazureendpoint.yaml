apiVersion: traffic.azure.upbound.io/v1alpha1
kind: ManagerAzureEndpoint
metadata:
  name: example
spec:
  forProvider:
    name: example-endpoint
    profileId: ${azurerm_traffic_manager_profile.example.id}
    targetResourceId: ${azurerm_public_ip.test.id}
    weight: 100
  providerConfigRef:
    name: example
