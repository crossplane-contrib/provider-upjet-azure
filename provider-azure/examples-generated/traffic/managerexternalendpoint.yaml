apiVersion: traffic.azure.upbound.io/v1beta1
kind: ManagerExternalEndpoint
metadata:
  name: example
spec:
  forProvider:
    name: example-endpoint
    profileId: ${azurerm_traffic_manager_profile.example.id}
    target: www.example.com
    weight: 100
  providerConfigRef:
    name: example
